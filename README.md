# ejay_pxd

pxd2wav converts PXD sample files from eJay applications to 16bit 44100kHz WAV files.
It converts both single PXD files, but also all PXD contained in batched files (accompanied by INF files).

Example usage: `pxd2wav <file>.[pxd|inf] [<file>.[pxd|inf]`
